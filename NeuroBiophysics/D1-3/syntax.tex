distance()               // Ursprung = center des aktuellen Sections
distance(0, x)          // setzt Origin am Abschnittsanfang
distance(x)             // Abstand vom gesetzten Origin bis zur Stelle x

for (x) stmt
für jeden Segment-Mittelpunkt (x) der aktuell ausgewählten Section
wird der Schleifenrumpf stmt einmal ausgeführt.
Dabei nimmt x Werte zwischen 0 und 1 entlang der Section an.